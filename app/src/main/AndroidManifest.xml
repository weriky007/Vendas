<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aulas.vendas">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vendas"
       >

        <activity android:name=".ui.activities.SplashScreenActivity" android:theme="@style/AppCompat.TelaCheia">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

         <activity android:name=".ui.activities.ListaDeClientesActivity"/>



        <activity android:name=".ui.activities.FormularioDadosPessoaisActivity"/>
        <activity android:name=".ui.activities.FormularioContatoActivity"/>
        <activity android:name=".ui.activities.FormularioEnderecoActivity"/>
        <activity android:name=".ui.activities.DadosClineteActivity"/>
        <activity android:name=".ui.activities.FormularioPacoteActivity"/>
    </application>
</manifest>